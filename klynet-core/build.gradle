plugins {
    alias(libs.plugins.shadow)
}

version = "1.1.0a"

dependencies {
    api libs.hikari.cp
    api libs.adventurousklynetmsg
    api libs.triumph.cmd
}

shadowJar {
    archiveClassifier.set("")
    relocate 'dev.triumphteam.cmd', 'it.klynet.klynetcore.cmd'
}

publishing {
    publications {
        maven(MavenPublication) {
            artifactId = 'klynet-core'
            // Use the shadowJar artifact
            artifact shadowJar.archiveFile.get()

            pom {
                name.set('klynet-core')
                description.set('klynet-core with relocated dependencies')
                url.set('http://84.217.92.154:25563/releases')
                scm {
                    connection.set('scm:git:git://84.217.92.154/klynet-core.git')
                    developerConnection.set('scm:git:ssh://84.217.92.154/klynet-core.git')
                    url.set('http://84.217.92.154/klynet-core')
                }
                licenses {
                    license {
                        name.set('The Apache License, Version 2.0')
                        url.set('http://www.apache.org/licenses/LICENSE-2.0.txt')
                    }
                }
                developers {
                    developer {
                        id.set('klynet')
                        name.set('Klyser8')
                        email.set('tomrom99@gmail.com')
                    }
                }
            }
        }
    }
    repositories {
        maven {
            name = "KlyNet"
            url = uri("http://84.217.92.154:25563/releases")
            credentials(PasswordCredentials)
            authentication {
                basic(BasicAuthentication)
            }
            allowInsecureProtocol = true
        }
    }
}